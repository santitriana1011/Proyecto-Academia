@model IEnumerable<myFirstAzureWebApp.Models.Traspaso>

@{
    ViewData["Title"] = "Traspasos";
}

@* 

        <p>
            <a asp-action="Create"><button class="btn btn-outline-primary mt-5">Nuevo</button></a>
        </p>
    <table class="table">

        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Estudiante.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Escuela.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaInicio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaFin)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Novedades)
                </td>
                <td>
                    
                </td>
            </tr>
    }
        </tbody>
    </table>*@

<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Business Casual - Start Bootstrap Theme</title>

    <!-- Bootstrap core CSS -->
    <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom fonts for this template -->
    <link href="https://fonts.googleapis.com/css?family=Raleway:100,100i,200,200i,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Lora:400,400i,700,700i" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="~/css/business-casual.min.css" rel="stylesheet">

</head>
<h2 class="mt-2" style="color:white; font-size:55px;">Seccion de Traspasos</h2>

<p>
    <a asp-action="Create"><button class="btn btn-primary mt-5" style="width:320px; margin-left:35%;">Agregar un Nuevo Traspaso</button></a>
</p>
<body>

    <section class="page-section clearfix">
        <div class="container">
            @foreach (var item in Model)
            {
                <div class="card mb-3">
                    <img src="~/images/fichajes1.jpg" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title"> @Html.DisplayFor(modelItem => item.Estudiante.Nombre)</h5>
                        <p class="card-text"> @Html.DisplayFor(modelItem => item.Escuela.Nombre)</p>
                        <p class="card-text"><small>@Html.DisplayFor(modelItem => item.FechaInicio)</small></p>
                        <p class="card-text"><small>@Html.DisplayFor(modelItem => item.FechaFin)</small></p>
                        <p class="card-text"><small>@Html.DisplayFor(modelItem => item.Novedades)</small></p>
                    </div>
                    <div>
                        <a asp-action="Edit" asp-route-id="@item.TraspasoID" class="btn btn-outline-success">Editar</a>
                        <a asp-action="Details" asp-route-id="@item.TraspasoID" class="btn btn-outline-warning">Detalles</a>
                        <a asp-action="Delete" asp-route-id="@item.TraspasoID" class="btn btn-outline-danger">Eliminar</a>
                    </div>
                </div>
            }
            </div>
    </section>

    <!-- Bootstrap core JavaScript -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>
