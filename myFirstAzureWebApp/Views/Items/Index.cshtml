@model IEnumerable<myFirstAzureWebApp.Models.Item>

@{
    ViewData["Title"] = "Item";
}

<h2 class="mt-2" style="color:white; font-size:55px;">Items Empresariales</h2>

<p>
    <a asp-action="Create"><button class="btn btn-primary mt-5" style="width:320px; margin-left:35%;">Agregar Nuevo Item</button></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th style="color:white;">
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th style="color:white;">
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th style="color:white;">
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td style="color:white;">
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td style="color:white;">
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td style="color:white;">
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ItemID" class="btn btn-outline-success">Editar</a> 
                <a asp-action="Details" asp-route-id="@item.ItemID" class="btn btn-outline-warning">Detalles</a> 
                <a asp-action="Delete" asp-route-id="@item.ItemID" class="btn btn-outline-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
