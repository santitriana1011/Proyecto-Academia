@model IEnumerable<myFirstAzureWebApp.Models.Estudiante>

@{
    ViewData["Title"] = "Estudiante";
}

<head>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load("current", { packages: ["corechart"] });
        google.charts.setOnLoadCallback(drawChart);
        function drawChart() {
            var data = google.visualization.arrayToDataTable([
                ['Task', 'Hours per Day'],
                ['Proximos a Competir Profesional', 11],
                ['Retirados', 2],
                ['Lesionados', 2],
                ['Recien Ingresados', 2],
                ['Trasladados', 7]
            ]);

            var options = {
                title: 'Balance General de Estudiantes',
                is3D: true,
            };

            var chart = new google.visualization.PieChart(document.getElementById('piechart_3d'));
            chart.draw(data, options);
        }
    </script>
</head>

<h2 class="mt-5">Estudiantes</h2>

<p>
    <a asp-action="Create"><button class="btn btn-outline-primary mt-5">Nuevo</button></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Acudiente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Documento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaNacimiento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Acudiente.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Documento)
                </td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Apellido)
                </td>
                <td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaNacimiento)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.EstudianteID" class="btn btn-outline-success">Editar</a> ||
                    <a asp-action="Details" asp-route-id="@item.EstudianteID" class="btn btn-outline-warning">Detalles</a> ||
                    <a asp-action="Delete" asp-route-id="@item.EstudianteID" class="btn btn-outline-danger">Eliminar</a>
                </td>
            </tr>*@
        }
    </tbody>
</table>
@*<h3>Segun el ultimo Balance las inscripciones subieron un 25%</h3><small><i>Publicado el 17 de Octubre</i></small>
<div class="bg-primary text-white p-2 my-2 w-50">25%</div>

<h3>Segun el balance los estudiantes retirados han bajado en un 5%</h3><small><i>Publicado el 17 de Octubre</i></small>
<div class="bg-danger text-white p-2 my-2 w-25">5%</div>*@

<div id="piechart_3d" style="width: 900px; height: 500px;"></div>
