@model IEnumerable<myFirstAzureWebApp.Models.Estudiante>
@{
    ViewData["Title"] = "Estudiante";
}


<style>
    .players {
        height: 300px;
        width: 500px;
    }
</style>

<p>
    <a asp-action="Create"><button class="btn btn-primary mt-5" style="width:25%; margin-left:35%;">Nuevo Estudiante</button></a>
</p>

<section>
    <div class="container">
        <div class="row mb-5">

            @foreach (var item in Model)
            {
                <div class="col-md-4 col-sm-6">
                    <div class="card text-center" style="max-width: 440px; display:inline-block; margin-right:12px; margin-left:24px; margin-top:9px;">
                        <div class="card-block">
                            <img src="@ViewData["FileLocation"]"  class="players img-fluid " />
                            <div class="card-title">
                                <h4>@Html.DisplayFor(modelItem => item.Nombre)</h4>
                                @Html.DisplayFor(modelItem => item.Apellido)
                            </div>
                            <div class="card-text">
                                <p>@Html.DisplayFor(modelItem => item.Acudiente.Nombre)</p>
                                <p>@Html.DisplayFor(modelItem => item.Documento)</p>
                                <p>@Html.DisplayFor(modelItem => item.FechaNacimiento)</p>
                            </div>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.EstudianteID" class="btn btn-outline-success">Editar</a> ||
                                <a asp-action="Details" asp-route-id="@item.EstudianteID" class="btn btn-outline-warning">Detalles</a> ||
                                <a asp-action="Delete" asp-route-id="@item.EstudianteID" class="btn btn-outline-danger">Eliminar</a>
                            </td>
                        </div>
                    </div>
                </div>
            }

        </div>
    </div>

</section>

<label>@ViewData["fname"]</label><br />




